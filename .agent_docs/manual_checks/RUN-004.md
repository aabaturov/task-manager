# Приёмка — RUN-004

**Спецификация:** SPEC-004 (внутри заголовок «SPEC-002»; артефакты — N=004)
**Версия:** 4.0
**Вердикт тестов:** PASSED (итерация 1) — `pytest` 102 passed, 0 failed
**Статус подъёма:** проект **локально НЕ поднят** — по запросу «без локального
докера». Backend проверен автотестами; фронтенд-сборку (vite) и живого бота
поднимаешь сам командой ниже.

## Как поднять и проверить локально

```bash
cd "C:\Else\study\AI projects\Task_manager"
docker compose -f docker-compose.local.yml up --build
```
Открой <http://localhost:8000> (Ctrl+F5 для свежего фронта). Вход — `WEB_LOGIN`/`WEB_PASSWORD` из `.env`.

> Бот: для напоминаний нужен extra `python-telegram-bot[job-queue]` — он уже
> добавлен в `backend/requirements.txt`, подтянется при сборке. Часовой пояс и
> время напоминания — переменные `APP_TIMEZONE` / `REMINDER_TIME` в `.env`
> (см. обновлённый `.env.example`).

Прогнать автотесты без докера:
```bash
cd backend && python -m venv .venv && .venv\Scripts\pip install -r requirements-dev.txt && .venv\Scripts\python -m pytest -q
```

## Что проверить (по сценариям спеки)

**Ф1 — верхняя зона и доска**
- [ ] Сверху три блока в ряд: «Дела на день» (жёлтый), «Лёгкие дела», «Календарь».
- [ ] Жёлтым выделён только «Дела на день»; два других — обычная схема.
- [ ] Доска ниже разбита на «временные» / «постоянные»; новый проект — временный.
- [ ] На телефоне (узкое окно) блоки перестраиваются в столбик.
- [ ] Старые проекты после миграции: «локальный»→«временный», «глобальный»→«постоянный».

**Ф2 — Лёгкие дела (не забыть)**
- [ ] Добавление непустого текста (пустой/пробелы — отказ; края тримятся).
- [ ] Отметка «сделано» (зачёркивание, остаётся), удаление; пустой список → заглушка.
- [ ] Не пересекается с задачами проектов и пометкой «важное».

**Ф3 — Календарь и события**
- [ ] Сетка месяца с событиями в клетках на их датах; листание месяцев.
- [ ] Клик по дню → форма (текст + время, время обязательно) → самостоятельное событие.
- [ ] Переполненная клетка раскрывается в полный список дня.
- [ ] Привязка к дате ≠ дедлайн (дедлайн в календаре не показывается).
- [ ] Событие-задача проекта: правка текста и статус «сделано» отражаются в календаре.
- [ ] Прошедшие события остаются на датах.

**Ф4 — Бот**
- [ ] При добавлении дела бот даёт выбор «привязать к дате» или проект; ветка даты создаёт событие (дата+время).
- [ ] Для задачи проекта бот доспрашивает про привязку к дате; «да» → задача в календаре.
- [ ] Кнопка «📝 Не забыть» присылает список лёгких дел (просмотр).
- [ ] Напоминание накануне; после простоя досылает сегодняшнее, устаревшие не шлёт.

## ⚠️ Открытые вопросы — нужно твоё решение
1. **Закреплённые проекты в новой раскладке.** Реализован дефолт из спеки —
   отдельная группа «закреплённые» над «временными»/«постоянными». Спека помечает
   это «на подтверждение заказчиком». Оставляем так или меняем?
2. **Жёлтый блок «Дела на день» vs ч/б редизайн (SPEC-003).** SPEC-003 сделал
   панель белой, SPEC-004 требует жёлтый — сейчас жёлтый (через `--day-bg`).
   Подтверждаешь жёлтый, или вернуть в общую ч/б схему?

## Полезное
```bash
docker compose -f docker-compose.local.yml logs -f
docker compose -f docker-compose.local.yml down
```
