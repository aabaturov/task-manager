# Отчёт тестировщика — TEST-004

**Вердикт:** PASSED
**Итерация:** 1

Файл `SPEC-004.md` (внутренний заголовок «SPEC-002», артефакты под N=004).
Прогон автотестов в backend-venv: **102 passed, 0 failed**. CRITICAL нет, упавших тестов нет — PASSED.

---

## Уровень 1 — Код-ревью

### Фича 1 — Перестройка верхней зоны и доски проектов
Полнота: выполнена.
- Три верхних блока в ряд — `Board.jsx:203-224` (контейнер `.top-blocks`): DayPanel, LightTasksPanel, CalendarPanel.
- Жёлтым выделён только «Дела на день»: `--day-bg: #fff8e1` (`styles.css:22`), используется только в `.day-panel` (`styles.css:481`); прочие блоки — нейтральный `.block`.
- Доска разбита на группы «Закреплённые → Временные → Постоянные», пустые группы не рендерятся, заголовки при >1 непустой группе — `Board.jsx:148-183, 226-231`.
- Типы переименованы на `temporary/permanent`, дефолт `temporary` — `schemas.py:15-21`, `models.py:34`, `Board.jsx:63`, `ProjectPanel.jsx:223-224`.
- Адаптив (столбик) при <=980px и <=640px — `styles.css:1043-1052`.

Граничные случаи закрыты (жёлтый только у дневной панели; пустые группы скрыты; дефолт временный; миграция local->temporary/global->permanent; закреплённые отдельной группой).

**SUGGESTION** (`Board.jsx:228`): группа «Закреплённые» над временными/постоянными — помеченный спекой пункт «на подтверждение заказчика». Реализован спека-дефолт; решает пользователь. Не CRITICAL.

**SUGGESTION** (`styles.css:22` vs SPEC-003): SPEC-003 сделал панель белой карточкой, SPEC-004 требует жёлтый. Конфликт двух спек; разработчик сменил значение переменной, не ломая тесты SPEC-003. Приоритет — за пользователем.

### Фича 2 — Лёгкие дела (не забыть)
Полнота: выполнена.
- Отдельная таблица `light_tasks` (текст, done, created_at, position), не связана с проектами/задачами/важным — `models.py:103-117`.
- API `GET/POST/PATCH/DELETE /api/light-tasks` — `main.py:362-432`. Пустой/пробельный текст отклоняется (`schemas.py:95` + `main.py:385-387`), trim краёв, порядок по created_at (`main.py:368-373`).
- UI: добавление, отметка done (зачёркивание, остаётся), удаление, заглушка «Нет дел» — `LightTasksPanel.jsx:18-69`.
- Независимость — тест `test_light_tasks_independent_from_project_tasks`. Создание только в вебе (бот-эндпойнта на создание нет).

### Фича 3 — Календарь и события
Полнота: выполнена.
- «Событие» = задача с `event_date`+`event_time`; `project_id` nullable — `models.py:53-71`.
- Привязка к дате независима от `deadline`; календарь питается только `event_date` (`main.py:440-441`). Тест `test_deadline_and_event_date_are_independent`.
- Время обязательно (`schemas.py:121`); очистка времени в PATCH запрещена (`main.py:526-530`, 422). Тесты на это зелёные.
- API: events диапазон/сортировка (`main.py:444-464`), POST только standalone (`main.py:467-492`), PATCH/DELETE для standalone, задача проекта -> 400 (`main.py:495-557`), `PUT /api/tasks/{id}/event` (оба поля вместе иначе 422 — `main.py:560-589`).
- Прошедшие события остаются; удаление задачи проекта убирает событие; снятие привязки оставляет обычную задачу; событие-задача отражает done/text — тесты зелёные.
- UI `CalendarPanel.jsx`: сетка месяца (Пн-первый), события в клетках, перелистывание, «Сегодня», модалка дня + форма создания (время обязательно, строки 170-173, 254); у событий-задач проекта чекбокс/удаление заблокированы (`disabled={!standalone}`, строки 204, 224).

**SUGGESTION** (`helpers.js:3-7, 27-29`): фронтенд считает «сегодня» и сетку в TZ браузера, спека требует единый TZ из `.env`. Даты хранятся как строки `YYYY-MM-DD` без сдвига, событие всегда в своей клетке; расхождение лишь в подсветке «сегодня» при TZ браузера != APP_TIMEZONE. Косметика.

**SUGGESTION** (`CalendarPanel.jsx`): инлайн-правка текста/времени существующего standalone-события из UI не сделана (API `PATCH /api/events/{id}` есть). Критерии Фичи 3 требуют создание/done/удаление (есть) и отражение правок события-задачи (есть); правка standalone из UI в критериях не названа — SUGGESTION.

### Фича 4 — Бот: события и просмотр «не забыть»
Полнота: выполнена.
- Поток: на текст бот предлагает «Привязать к дате» или проект (`bot.py:188-203`). Ветка даты -> дата, затем время, standalone-событие (`bot.py:206-278`). Ветка проекта -> задача в проект, затем «Привязать к дате?» да/нет (`bot.py:281-337`); «да» привязывает дату+время к задаче проекта (`bot.py:251-261`).
- Кнопка «Не забыть» — список лёгких дел, только просмотр, при пустом уведомление (`bot.py:488-518`). Клавиатура с третьей кнопкой (`bot.py:66-72`, тест `test_reply_keyboard_layout`).
- Парсеры даты/времени переспрашивают (`bot.py:114-137, 224-244`), время обязательно.
- Планировщик: ежедневный job в REMINDER_TIME/APP_TIMEZONE (`bot.py:567-589`); досыл при старте, если время сегодня прошло (`run_once(when=1)`), старые пропускаются. «Накануне» = события на `_today()+1` (`bot.py:553-564`) -> событие на сегодня не напоминается (v1).
- Тело напоминания: None без событий, сортировка по времени, проекты+standalone, done (`bot.py:532-550`, тесты `test_reminder_*`). Доступ только владельцу (`bot.py:147-151`).

**WARNING** (`bot.py:574-576`): без extra `[job-queue]` `app.job_queue` = None и напоминания тихо отключаются (warning в лог). В рантайме зависимость есть (`requirements.txt:9`), в проверочном venv установлена — JobQueue доступен. Риск только при ручной установке без extra. На критерии приёмки не влияет.

### Совместимость / миграция
- Переименование типов в существующей БД — `database.py:111-117` (тест `test_spec001_type_values_renamed`).
- Снятие NOT NULL с `tasks.project_id` через rebuild для SQLite — `database.py:48-84, 144-145`; данные переносятся (тест `test_v1_database_migrates`).
- Новые колонки событий и `light_tasks` добавляются идемпотентно. Старые задачи без даты в календарь не попадают (`main.py:440-441`). Данные SPEC-001 сохранены.

---

## Уровень 2 — Автотесты

Окружение: backend-venv (`backend/.venv`), зависимости из `requirements-dev.txt` установлены (fastapi, sqlalchemy, pydantic, python-telegram-bot[job-queue], httpx, pytest). Docker не использовался.

Команда: `cd backend && .venv/Scripts/python.exe -m pytest -q`
Результат: **102 passed, 0 failed** (4.78s).

Покрытие:
- `test_spec004_api.py` — типы проектов (rename/дефолт/отказ старых значений), лёгкие дела (CRUD/trim/пусто/независимость), события (standalone, обязательность времени, диапазон, сортировка, правка/удаление, прошедшие даты), привязка задачи проекта (event, снятие, независимость от дедлайна, удаление, done/текст, отказ event-роутов для задачи проекта).
- `test_bot_spec004.py` — парсинг даты/времени, REMINDER_TIME, «Не забыть» (пусто/порядок/done/escape), тело напоминания.
- `test_migration.py` — новые колонки, nullable project_id, light_tasks, переименование типов, сохранность данных.
- `test_api.py`, `test_bot.py`, `test_daypanel_spec003.py` — регрессы зелёные.

### Непокрытые автотестами сценарии (визуальная/ручная проверка)
- Фронтенд (React/vite) не собирался — рендер блоков, адаптив-столбик, раскрытие переполненной клетки, модалка дня.
- Живая отправка напоминаний через Telegram JobQueue (покрыты только билдеры сообщений и расписание-хелперы).
- Сквозной поток бота с реальными Update/CallbackQuery (хендлеры покрыты на уровне helper-функций).

---

## Итог

SPEC-004 реализована полностью по всем 4 фичам и техтребованиям. CRITICAL-несоответствий нет, упавших тестов нет. Замечания — SUGGESTION (закреплённые проекты «на подтверждение», жёлтый блок vs SPEC-003, TZ браузера на фронте, инлайн-правка standalone-события) и один WARNING (JobQueue требует extra, в рантайме присутствует). Все они отданы на решение пользователя либо не затрагивают критерии приёмки.

**Вердикт: PASSED.** Можно готовить локальный запуск (с визуальной проверкой фронтенда и живого бота).
