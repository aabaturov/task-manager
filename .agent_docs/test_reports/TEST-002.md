# Отчёт тестировщика — TEST-002

**Вердикт:** PASSED
**Итерация:** 1

## Что проверялось
SPEC-002 (две фичи: постоянная reply-клавиатура и read-only экран «Дела на день»)
против реализации в `backend/bot.py` и автотестов `backend/tests/test_bot.py`.

## Уровень 1 — Код-ревью

### Фича 1: Постоянная клавиатура
- Критерий «`/start` показывает приветствие + клавиатуру с двумя кнопками в один
  ряд» — выполнено. `_reply_keyboard()` (`bot.py:50-56`) возвращает
  `ReplyKeyboardMarkup([[BTN_TASKS, BTN_DAY]], resize_keyboard=True, is_persistent=True)`;
  `start` шлёт приветствие с этой клавиатурой (`bot.py:87-96`).
- «`📋 Все задачи` = вывод `/tasks`» — выполнено: `show_all_tasks` и `tasks` оба
  вызывают `_send_task_list(update.message.reply_text)` (`bot.py:156-167`),
  вывод идентичен.
- «`📅 Дела на день` открывает экран Фичи 2» — выполнено (`day_screen`, `bot.py:303-308`).
- «Произвольный текст создаёт задачу» — выполнено: общий
  `MessageHandler(filters.TEXT & ~filters.COMMAND, receive_text)` (`bot.py:327-329`).
- «Нажатие кнопки не создаёт задачу» — выполнено: точные обработчики
  `filters.Text([BTN_TASKS])` / `filters.Text([BTN_DAY])` зарегистрированы
  ПЕРЕД общим обработчиком (`bot.py:325-329`). Порядок строгий, как в спеке
  (граничный случай F1).
- «Подписи кнопок = строки фильтров (единые константы)» — выполнено: `BTN_TASKS`,
  `BTN_DAY` (`bot.py:40-41`) используются и в клавиатуре, и в фильтрах.
- «Не-владелец не получает ни клавиатуру, ни данные» — выполнено: `_is_owner`
  проверяется на всех новых точках входа — `start` (`bot.py:88`),
  `show_all_tasks` (`bot.py:164`), `day_screen` (`bot.py:305`). При
  `telegram_allowed_user_id is None` доступ закрыт (`bot.py:81-82`).
- «Бот импортируется без ошибок» — выполнено (проверено `import bot` — OK).
- Повторный `/start` идемпотентен (только шлёт клавиатуру) — соответствует.

### Фича 2: Экран «Дела на день»
- «Тот же источник, что `DayPanel`; новых таблиц/полей нет» — выполнено.
  `_read_slots` (`bot.py:224-243`) посимвольно зеркалит `app.main.get_day_slots`
  (`main.py:301-311`): тот же запрос `DaySlotItem.order_by(slot_index, position)`,
  тот же `SLOT_COUNT = 3`, та же фильтрация `0 <= slot_index < SLOT_COUNT`.
- «Непустые слоты по порядку index, заголовок `Слот N` 1-based» — выполнено:
  `header = f"<b>Слот {slot['index'] + 1}</b>"` (`bot.py:280`).
- «Каждая задача: подпись проекта + текст» — выполнено (`bot.py:269-272`),
  `_label` зеркалит веб (эмодзи + имя).
- «Выполненные — `✅` + зачёркивание» — выполнено: `✅ <s>{body}</s>`
  (`bot.py:273-274`).
- «Экранирование `< > &`» — выполнено: `_esc` через `html.escape(..., quote=False)`
  применяется к подписи проекта и тексту задачи (`bot.py:270-271`);
  собственная разметка `<b>`, `<s>` не экранируется. `parse_mode=HTML`
  (`bot.py:308`).
- «Пустые слоты не выводятся» — выполнено (`bot.py:277-279`).
- «Ничего не выбрано → "На день пока ничего не выбрано"» — выполнено
  (`bot.py:283-284`).
- «Только просмотр, кнопок отметки нет» — выполнено: `day_screen` шлёт
  только текст без `reply_markup` (`bot.py:307-308`).
- «Висячий id не роняет экран» — выполнено: `task_by_id.get(tid)` → `None`
  пропускается (`bot.py:265-268`); слот, опустевший из-за этого, пропускается
  (`bot.py:277-279`).
- «Длинный список не вызывает ошибку Telegram (4096)» — выполнено: блоки
  пакуются в сообщения без превышения `TELEGRAM_MAX_LEN`, слот не рвётся
  посередине (`bot.py:286-300`).

Критических несоответствий не найдено.

## Уровень 2 — Автотесты

Команда проекта: `cd backend && ./.venv/Scripts/python.exe -m pytest -q`

Результат: **37 passed, 0 failed** (2.41s). Из них 14 новых тестов по SPEC-002
(`tests/test_bot.py`) + 23 регрессионных существующих — все зелёные.

Покрытие SPEC-002 тестами:
- F1: константы кнопок, раскладка клавиатуры (`resize_keyboard`, `is_persistent`,
  две кнопки в ряд), проверка владельца (включая `effective_user is None`).
- F2: пустое состояние; форма `_read_slots` (совпадение с веб-схемой и порядком);
  рендер с подписью проекта и порядком слотов; пометка/зачёркивание выполненной;
  отсутствие пометки у невыполненной; экранирование `< > &`; пропуск пустого
  слота; пропуск висячего id; пропуск слота, опустевшего из-за висячих id;
  разбиение длинного вывода по границам слотов в пределах 4096; чистый импорт.

Линтер/типизация: в проекте не сконфигурированы (нет pyproject/setup/конфигов
линтера; `requirements-dev.txt` содержит только pytest + httpx). Принятая в
проекте проверка — pytest — пройдена.

## Замечания

- **MINOR (не блокирует):** В тесте `test_button_constants_are_used_in_filters`
  (`test_bot.py:56-59`) проверяется только равенство констант литералам, но не
  то, что именно эти константы переданы в `filters.Text([...])` при регистрации
  хендлеров. Код регистрации (`bot.py:325-326`) использует те же константы, так
  что фактического дефекта нет; это пробел в формулировке теста (имя обещает
  «used in filters»), а не баг реализации.
- **MINOR (соответствует спеке, отмечаю явно):** Строки `📋 Все задачи` и
  `📅 Дела на день` нельзя сохранить как текст задачи — осознанный трейдоф,
  явно зафиксированный в SPEC-002 (F1, граничные случаи). Не дефект.

## Непокрытые сценарии
Существенных непокрытых сценариев из спеки не выявлено. Интеграционная отправка
в Telegram (реальный API) автотестами не проверяется — это ожидаемо для unit-слоя
и относится к ручной приёмке (RUN-002).

## Итог
Все критерии приёмки SPEC-002 выполнены, критических замечаний и упавших тестов
нет. Вердикт — PASSED.
